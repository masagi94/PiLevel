This project consists of using an accelerometer and an LCD, along side a 
raspberry pi 3, to make a simple level. The accelerometer is intened to
be attached to a ruler (or some type of straight edge) and will display
the current orientation of the ruler in degrees, or radians.